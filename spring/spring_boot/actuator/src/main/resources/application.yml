management:
  server:
    port: 9292
  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
    git:
      mode: "full"
  endpoint:
    shutdown:
      enabled: true
    health:
        #show-details: always
        show-components: always

  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: hello-actuator
    company: yh

logging:
  level:
    hello.controller: debug